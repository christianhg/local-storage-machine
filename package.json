{
  "name": "local-storage-machine",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --minify --external react --external xstate",
    "test": "vitest run",
    "type-check": "tsc --pretty --noEmit"
  },
  "devDependencies": {
    "tsup": "^6.6.0",
    "typescript": "^4.9.5",
    "vitest": "^0.28.4"
  },
  "dependencies": {
    "xstate": "^4.35.4",
    "zod": "^3.20.3"
  }
}
